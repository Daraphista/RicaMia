module.exports = {
  theme: {
    extend: {
      fontFamily: {
        garet: ["var(--font-garet)", "sans-serif"], // Use the CSS variable
        brittany: ["var(--font-brittany)", "serif"], // Use the CSS variable
      },
    },
  },
};
